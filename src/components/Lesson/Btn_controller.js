import PropTypes from 'prop-types'
import React, { Component } from 'react'

export default class Btn_controller extends Component {
  static propTypes = {second: "third"}

  render() {
    return (
      <div>Btn_controller</div>
    )
  }
}
